#include <stdio.h>

int main() {

    // We are going to learn modulus operator
    int a = 10;
    int b = 3;

    int mod = a % b;

    printf("%d", mod);

    // format specifiers
    // %d => integer;
    // %s => string;
    // %c => character;
    // %f => float;
    // %lf => double;
    // %u => unsigned integer;
    // %x => hexadecimal integer;
    // %X => hexadecimal integer (uppercase);
    // %lu => long unsigned integer;
    


    
    return 0;
}