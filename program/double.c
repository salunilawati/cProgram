#include <stdio.h>
int main()
{

    double k, l;//declaration 
    printf("Enter two numbers: \n");
    scanf("%lf %lf", &k, &l);
    double sum6 = k + l;
    printf("The sum of the numbers is: %lf\n", sum6);
    return 0;
}